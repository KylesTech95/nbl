<style>
    *{
        list-style: none;
        margin:0;
        padding:0;
        box-sizing:border-box;
    }

    form#nbl_ad_auth{
        /* border:2px solid red; */
        min-height:350px;
        max-height:100%;
        width:50%;
        border-radius: 20px;
        background:transparent;
        display:flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap:1rem;
        z-index: 999;
    }
    form#nbl_ad_auth>div{
        /* border:2px solid rgb(192, 100, 100); */
        display:inherit;
        flex-direction: inherit;
        width:100%;
        /* border:3px solid green; */
        flex-direction: inherit;
        justify-content: center;
        align-items: center;
    }
form#nbl_ad_auth>div>input{
    width: 66%;
    height:25px;
    padding:.5rem .25rem;
    font-size:16pt;
    padding:1.55rem;
}
input[type='submit']{
    padding:.5rem 1rem;
    border:none;
    width:50%;
    border-radius: 12px;
    transition: .25s;
    box-shadow: 0 .25rem 5px .9px #000;
    font-size:16pt;
}
#forgot-pw,
#existing-login,
#newcomer{
    transition: .75s;
    padding:6px;
    border-radius: 12px;
    border:none;
}

#forgot-pw:hover,
#existing-login:hover,
#newcomer:hover{
    cursor:pointer;
    color:#000;
    background:#fff;
    transition: .25s;
}
input[type='submit']:hover{
    cursor:pointer;
    box-shadow: inset 0 -1rem 28px .9px #000;
    color:#fff;
    transform:scale(.98);
    letter-spacing: 1.5px;
}
#footer{
    position:absolute;
    bottom:0;
    left:0;
}
#main-title.auth{
    margin-top:1.75rem;
}
#admin-label{
    font-family: Arial, Helvetica, sans-serif;
    color:#f00;
    position: absolute;
    right:0;
    top:0;
    font-size:18pt;
    padding:.25rem .5rem;
    border-radius: 20px 0 0 0;
    border:8px double ghostwhite;
    border-top:none;
    border-right:none;
    letter-spacing: 2.25px;
}
#auth-foreign-contaner{
    /* bordeR:2px solid red; */
    padding:.5rem;
    display:flex;
    align-items: start;
    justify-content: space-evenly;
    margin-top:1rem;
    gap:1rem;
}
#or-container>p{
    color:#fff;
    margin-top:1.5rem;
}
.foreign-link{
    display:flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
    font-size:11pt;
    letter-spacing: 1.25px;
}
.logo-foreign{
    width:75px;
    transition: .75s;
}
.logo-foreign:hover{
    width:65px;
    transition: .25s;
    cursor:pointer;
}
@media screen and (max-width:670px){
    form#nbl_ad_auth{
        width:88%;
        gap:5px;
    }
    form#nbl_ad_auth>div{
        align-items: center;
    }

}

</style>